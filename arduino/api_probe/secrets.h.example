/*
 * WiFi 凭据模板
 * ================================================================
 * 用法：把本文件复制成同目录下的 secrets.h，填上你自己的 WiFi。
 *
 *     cp secrets.h.example secrets.h
 *
 * secrets.h 已在 .gitignore 里，**不会进版本库** —— 别把真密码提交上去。
 * 每个需要联网的 Demo 目录下都有一份这个模板，用到哪个就复制哪个。
 *
 * ⚠️ ESP32-S3 只支持 2.4 GHz。5G 专属 SSID 连不上；
 *    双频合一（2.4G/5G 同名）一般没问题。
 * ⚠️ 需要网页认证的网络（校园网 / 酒店）连不上 —— 那要在浏览器里登录，
 *    WiFi.begin() 绕不过去。
 */

#pragma once

#define WIFI_SSID "你的WiFi名"
#define WIFI_PASS "你的WiFi密码"
