# ---------------------------------------------------------------
# 敏感凭据 —— 绝对不要提交
# ---------------------------------------------------------------
# 每个联网例程目录下的真凭据。clone 的人照 secrets.h.example 自己填。
secrets.h
# 本地 WiFi 备忘录（明文密码）
wifi.md
# 递归兜底：万一以后加了子目录
**/secrets.h

# ---------------------------------------------------------------
# 编译产物
# ---------------------------------------------------------------
build/
*.bin
# 例外：slideshow 的示例图是要跟着仓库走的，别被上面的 *.bin 挡掉。
# 反向规则必须写在 *.bin 后面才生效。
!arduino/photo_slideshow/data/*.bin
*.elf
*.hex
*.map
*.d
*.o
*.a
# Python（tools/ 下跑脚本会生成）
__pycache__/
*.pyc

# ---------------------------------------------------------------
# 编辑器 / 系统
# ---------------------------------------------------------------
.DS_Store
.vscode/
*.swp
# Claude Code 的本机权限白名单，只对这台机器有意义
.claude/settings.local.json
